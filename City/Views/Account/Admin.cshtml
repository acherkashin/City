@model City.Models.AdminViewModel
@using City.Utils

@{
    ViewData["Title"] = "Admin";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<div>
    <table class="online-users table table-striped">
        <thead>
            <tr>
                <th>Статус</th>
                <th>Имя</th>
                <th>Фамилия</th>
                <th>Логин</th>
                <th>Объект</th>
            </tr>
        </thead>
        <tbody>
            @foreach (User user in Model.Users)
            {
                <tr class="online-user">
                    <td class="online-user__status"></td>
                    <td>@user.FirstName</td>
                    <td>@user.LastName</td>
                    <td>@user.Login</td>
                    <td>@user.Subject.GetDisplayName()</td>
                </tr>
            }
        </tbody>
    </table>
</div>
<script>
    $(document).ready(() => {
        new NetClient({
            hubUrl: 'http://@Context.Request.Host/net',
            onRecieve: (packate) => {

            },
            onUpdateOnlineList: (users) => {

            }
        })
    })
</script>
